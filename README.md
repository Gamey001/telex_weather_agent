# 🌦 Telex Weather Agent (FastAPI)

An AI agent built for **HNG Stage 3 Backend Task**, integrated with **Telex.im**.  
It fetches and responds with live weather info for any location.

## 🧠 Features

- Built with FastAPI
- Deployed via Render
- Uses OpenWeather API
- Compatible with Telex A2A protocol

## 🚀 Setup

```bash
git clone https://github.com/Gamey001/telex_weather_agent.git
cd telex-weather-agent
pip install -r requirements.txt
uvicorn app.main:app --reload

```
